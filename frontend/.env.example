# Optional. Defaults to 3000 when running `npm run dev`.
PORT=3000

# Local development may access Ollama or other services on private networks.
ALLOW_PRIVATE_MODEL_HOSTS=true
API_RATE_LIMIT_PER_MINUTE=120
